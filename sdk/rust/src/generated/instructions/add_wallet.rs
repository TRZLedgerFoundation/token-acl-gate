//! This code was AUTOGENERATED using the codoma library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun codoma to update it.
//!
//! <https://github.com/trzledgerfoundation-idl/codoma>
//!

use borsh::BorshDeserialize;
use borsh::BorshSerialize;

pub const ADD_WALLET_DISCRIMINATOR: u8 = 2;

/// Accounts.
#[derive(Debug)]
pub struct AddWallet {
    pub authority: trezoa_pubkey::Pubkey,

    pub list_config: trezoa_pubkey::Pubkey,

    pub wallet: trezoa_pubkey::Pubkey,

    pub wallet_entry: trezoa_pubkey::Pubkey,

    pub system_program: trezoa_pubkey::Pubkey,
}

impl AddWallet {
    pub fn instruction(&self) -> trezoa_instruction::Instruction {
        self.instruction_with_remaining_accounts(&[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::vec_init_then_push)]
    pub fn instruction_with_remaining_accounts(
        &self,
        remaining_accounts: &[trezoa_instruction::AccountMeta],
    ) -> trezoa_instruction::Instruction {
        let mut accounts = Vec::with_capacity(5 + remaining_accounts.len());
        accounts.push(trezoa_instruction::AccountMeta::new(self.authority, true));
        accounts.push(trezoa_instruction::AccountMeta::new(
            self.list_config,
            false,
        ));
        accounts.push(trezoa_instruction::AccountMeta::new_readonly(
            self.wallet,
            false,
        ));
        accounts.push(trezoa_instruction::AccountMeta::new(
            self.wallet_entry,
            false,
        ));
        accounts.push(trezoa_instruction::AccountMeta::new_readonly(
            self.system_program,
            false,
        ));
        accounts.extend_from_slice(remaining_accounts);
        let data = borsh::to_vec(&AddWalletInstructionData::new()).unwrap();

        trezoa_instruction::Instruction {
            program_id: crate::ABL_ID,
            accounts,
            data,
        }
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct AddWalletInstructionData {
    discriminator: u8,
}

impl AddWalletInstructionData {
    pub fn new() -> Self {
        Self { discriminator: 2 }
    }
}

impl Default for AddWalletInstructionData {
    fn default() -> Self {
        Self::new()
    }
}

/// Instruction builder for `AddWallet`.
///
/// ### Accounts:
///
///   0. `[writable, signer]` authority
///   1. `[writable]` list_config
///   2. `[]` wallet
///   3. `[writable]` wallet_entry
///   4. `[optional]` system_program (default to `11111111111111111111111111111111`)
#[derive(Clone, Debug, Default)]
pub struct AddWalletBuilder {
    authority: Option<trezoa_pubkey::Pubkey>,
    list_config: Option<trezoa_pubkey::Pubkey>,
    wallet: Option<trezoa_pubkey::Pubkey>,
    wallet_entry: Option<trezoa_pubkey::Pubkey>,
    system_program: Option<trezoa_pubkey::Pubkey>,
    __remaining_accounts: Vec<trezoa_instruction::AccountMeta>,
}

impl AddWalletBuilder {
    pub fn new() -> Self {
        Self::default()
    }
    #[inline(always)]
    pub fn authority(&mut self, authority: trezoa_pubkey::Pubkey) -> &mut Self {
        self.authority = Some(authority);
        self
    }
    #[inline(always)]
    pub fn list_config(&mut self, list_config: trezoa_pubkey::Pubkey) -> &mut Self {
        self.list_config = Some(list_config);
        self
    }
    #[inline(always)]
    pub fn wallet(&mut self, wallet: trezoa_pubkey::Pubkey) -> &mut Self {
        self.wallet = Some(wallet);
        self
    }
    #[inline(always)]
    pub fn wallet_entry(&mut self, wallet_entry: trezoa_pubkey::Pubkey) -> &mut Self {
        self.wallet_entry = Some(wallet_entry);
        self
    }
    /// `[optional account, default to '11111111111111111111111111111111']`
    #[inline(always)]
    pub fn system_program(&mut self, system_program: trezoa_pubkey::Pubkey) -> &mut Self {
        self.system_program = Some(system_program);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(&mut self, account: trezoa_instruction::AccountMeta) -> &mut Self {
        self.__remaining_accounts.push(account);
        self
    }
    /// Add additional accounts to the instruction.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[trezoa_instruction::AccountMeta],
    ) -> &mut Self {
        self.__remaining_accounts.extend_from_slice(accounts);
        self
    }
    #[allow(clippy::clone_on_copy)]
    pub fn instruction(&self) -> trezoa_instruction::Instruction {
        let accounts = AddWallet {
            authority: self.authority.expect("authority is not set"),
            list_config: self.list_config.expect("list_config is not set"),
            wallet: self.wallet.expect("wallet is not set"),
            wallet_entry: self.wallet_entry.expect("wallet_entry is not set"),
            system_program: self
                .system_program
                .unwrap_or(trezoa_pubkey::pubkey!("11111111111111111111111111111111")),
        };

        accounts.instruction_with_remaining_accounts(&self.__remaining_accounts)
    }
}

/// `add_wallet` CPI accounts.
pub struct AddWalletCpiAccounts<'a, 'b> {
    pub authority: &'b trezoa_account_info::AccountInfo<'a>,

    pub list_config: &'b trezoa_account_info::AccountInfo<'a>,

    pub wallet: &'b trezoa_account_info::AccountInfo<'a>,

    pub wallet_entry: &'b trezoa_account_info::AccountInfo<'a>,

    pub system_program: &'b trezoa_account_info::AccountInfo<'a>,
}

/// `add_wallet` CPI instruction.
pub struct AddWalletCpi<'a, 'b> {
    /// The program to invoke.
    pub __program: &'b trezoa_account_info::AccountInfo<'a>,

    pub authority: &'b trezoa_account_info::AccountInfo<'a>,

    pub list_config: &'b trezoa_account_info::AccountInfo<'a>,

    pub wallet: &'b trezoa_account_info::AccountInfo<'a>,

    pub wallet_entry: &'b trezoa_account_info::AccountInfo<'a>,

    pub system_program: &'b trezoa_account_info::AccountInfo<'a>,
}

impl<'a, 'b> AddWalletCpi<'a, 'b> {
    pub fn new(
        program: &'b trezoa_account_info::AccountInfo<'a>,
        accounts: AddWalletCpiAccounts<'a, 'b>,
    ) -> Self {
        Self {
            __program: program,
            authority: accounts.authority,
            list_config: accounts.list_config,
            wallet: accounts.wallet,
            wallet_entry: accounts.wallet_entry,
            system_program: accounts.system_program,
        }
    }
    #[inline(always)]
    pub fn invoke(&self) -> trezoa_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], &[])
    }
    #[inline(always)]
    pub fn invoke_with_remaining_accounts(
        &self,
        remaining_accounts: &[(&'b trezoa_account_info::AccountInfo<'a>, bool, bool)],
    ) -> trezoa_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], remaining_accounts)
    }
    #[inline(always)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> trezoa_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(signers_seeds, &[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed_with_remaining_accounts(
        &self,
        signers_seeds: &[&[&[u8]]],
        remaining_accounts: &[(&'b trezoa_account_info::AccountInfo<'a>, bool, bool)],
    ) -> trezoa_program_error::ProgramResult {
        let mut accounts = Vec::with_capacity(5 + remaining_accounts.len());
        accounts.push(trezoa_instruction::AccountMeta::new(
            *self.authority.key,
            true,
        ));
        accounts.push(trezoa_instruction::AccountMeta::new(
            *self.list_config.key,
            false,
        ));
        accounts.push(trezoa_instruction::AccountMeta::new_readonly(
            *self.wallet.key,
            false,
        ));
        accounts.push(trezoa_instruction::AccountMeta::new(
            *self.wallet_entry.key,
            false,
        ));
        accounts.push(trezoa_instruction::AccountMeta::new_readonly(
            *self.system_program.key,
            false,
        ));
        remaining_accounts.iter().for_each(|remaining_account| {
            accounts.push(trezoa_instruction::AccountMeta {
                pubkey: *remaining_account.0.key,
                is_signer: remaining_account.1,
                is_writable: remaining_account.2,
            })
        });
        let data = borsh::to_vec(&AddWalletInstructionData::new()).unwrap();

        let instruction = trezoa_instruction::Instruction {
            program_id: crate::ABL_ID,
            accounts,
            data,
        };
        let mut account_infos = Vec::with_capacity(6 + remaining_accounts.len());
        account_infos.push(self.__program.clone());
        account_infos.push(self.authority.clone());
        account_infos.push(self.list_config.clone());
        account_infos.push(self.wallet.clone());
        account_infos.push(self.wallet_entry.clone());
        account_infos.push(self.system_program.clone());
        remaining_accounts
            .iter()
            .for_each(|remaining_account| account_infos.push(remaining_account.0.clone()));

        if signers_seeds.is_empty() {
            trezoa_cpi::invoke(&instruction, &account_infos)
        } else {
            trezoa_cpi::invoke_signed(&instruction, &account_infos, signers_seeds)
        }
    }
}

/// Instruction builder for `AddWallet` via CPI.
///
/// ### Accounts:
///
///   0. `[writable, signer]` authority
///   1. `[writable]` list_config
///   2. `[]` wallet
///   3. `[writable]` wallet_entry
///   4. `[]` system_program
#[derive(Clone, Debug)]
pub struct AddWalletCpiBuilder<'a, 'b> {
    instruction: Box<AddWalletCpiBuilderInstruction<'a, 'b>>,
}

impl<'a, 'b> AddWalletCpiBuilder<'a, 'b> {
    pub fn new(program: &'b trezoa_account_info::AccountInfo<'a>) -> Self {
        let instruction = Box::new(AddWalletCpiBuilderInstruction {
            __program: program,
            authority: None,
            list_config: None,
            wallet: None,
            wallet_entry: None,
            system_program: None,
            __remaining_accounts: Vec::new(),
        });
        Self { instruction }
    }
    #[inline(always)]
    pub fn authority(&mut self, authority: &'b trezoa_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.authority = Some(authority);
        self
    }
    #[inline(always)]
    pub fn list_config(
        &mut self,
        list_config: &'b trezoa_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.list_config = Some(list_config);
        self
    }
    #[inline(always)]
    pub fn wallet(&mut self, wallet: &'b trezoa_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.wallet = Some(wallet);
        self
    }
    #[inline(always)]
    pub fn wallet_entry(
        &mut self,
        wallet_entry: &'b trezoa_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.wallet_entry = Some(wallet_entry);
        self
    }
    #[inline(always)]
    pub fn system_program(
        &mut self,
        system_program: &'b trezoa_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.system_program = Some(system_program);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(
        &mut self,
        account: &'b trezoa_account_info::AccountInfo<'a>,
        is_writable: bool,
        is_signer: bool,
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .push((account, is_writable, is_signer));
        self
    }
    /// Add additional accounts to the instruction.
    ///
    /// Each account is represented by a tuple of the `AccountInfo`, a `bool` indicating whether the account is writable or not,
    /// and a `bool` indicating whether the account is a signer or not.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[(&'b trezoa_account_info::AccountInfo<'a>, bool, bool)],
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .extend_from_slice(accounts);
        self
    }
    #[inline(always)]
    pub fn invoke(&self) -> trezoa_program_error::ProgramResult {
        self.invoke_signed(&[])
    }
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> trezoa_program_error::ProgramResult {
        let instruction = AddWalletCpi {
            __program: self.instruction.__program,

            authority: self.instruction.authority.expect("authority is not set"),

            list_config: self
                .instruction
                .list_config
                .expect("list_config is not set"),

            wallet: self.instruction.wallet.expect("wallet is not set"),

            wallet_entry: self
                .instruction
                .wallet_entry
                .expect("wallet_entry is not set"),

            system_program: self
                .instruction
                .system_program
                .expect("system_program is not set"),
        };
        instruction.invoke_signed_with_remaining_accounts(
            signers_seeds,
            &self.instruction.__remaining_accounts,
        )
    }
}

#[derive(Clone, Debug)]
struct AddWalletCpiBuilderInstruction<'a, 'b> {
    __program: &'b trezoa_account_info::AccountInfo<'a>,
    authority: Option<&'b trezoa_account_info::AccountInfo<'a>>,
    list_config: Option<&'b trezoa_account_info::AccountInfo<'a>>,
    wallet: Option<&'b trezoa_account_info::AccountInfo<'a>>,
    wallet_entry: Option<&'b trezoa_account_info::AccountInfo<'a>>,
    system_program: Option<&'b trezoa_account_info::AccountInfo<'a>>,
    /// Additional instruction accounts `(AccountInfo, is_writable, is_signer)`.
    __remaining_accounts: Vec<(&'b trezoa_account_info::AccountInfo<'a>, bool, bool)>,
}
